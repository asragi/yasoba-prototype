# Repository Guidelines

## ビルド・テスト・開発コマンド

agent作業終了後は以下のコマンドを実行してください
- `go fmt ./...`
- `go build ./...`
- `go test ./...`
- `go generate ./...`


## 実装方針

副作用のない純粋関数をベースに実装することを前提としてください。
UpdateやDrawから呼び出される想定の処理の中ではメモリアロケーションの無いように注意を払ってください。
可能な限り package 外部の依存を直接参照せず、複数メソッドを扱う場合は interface を、単一の関数だけ使う場合は `type Func() ...` 形式の関数型エイリアスで注入できるように抽象化してください。

wire_gen.goは編集せず，wire.goを編集してから`go generate ./...`を実行することで生成してください．

go.sumやgo.modは編集しないでください
