# Repository Guidelines

## プロジェクト構成とモジュール整理
プロトタイプは `main.go` を中心とした Go モジュールで、Ebiten のシーン遷移を統括します。戦闘ロジックやアクター処理は `core/`、ウィンドウやダメージ表示など画面側の要素は `component/` と `scene/` にまとまっています。色計算・入力平滑化・描画ユーティリティは `frontend/` と `widget/` に配置され、会話や連携イベントなどの進行は `sequence/` で制御されます。イベントデータやセリフは YAML として `data/` に置かれ、画像・フォントは `image/` と `font/`、デバッグ補助は `debug/` で管理します。

## ビルド・テスト・開発コマンド
- `go run main.go` : 現在のバトルプロトタイプを起動し、手動プレイテストを行います。
- `go build ./...` : 依存を含めたビルドを実行し、実行可能ファイルをワーキングディレクトリに生成します。
- `go test ./...` : すべての Go テストを走らせ、イベントアダプタや UI ヘルパーの回帰を早期検知します。
- `go test -run Sequence ./sequence` : シーケンス周りに変更がある場合に対象テストだけを素早く回します。

## コーディングスタイルと命名規約
基本は Go の慣習に従い、コミット前に `go fmt ./...` を必ず実行します。パッケージ名は `core` や `util` のようにスネークケースを維持し、外部公開が必要なものだけをエクスポートします。公開シンボルはパスカルケース、内部用はキャメルケースとし、`battle_enemy_display.go` のように責務をファイル単位で明確に分割します。描画処理を追加する際は Ebiten の draw 呼び出しを論理単位でまとめ、UI 更新の追跡を容易に保ちます。

## テスト指針
ユニットテストは Go の `testing` パッケージと YAML フィクスチャを組み合わせて構成します。仕様追加時は実装ファイルに隣接する `_test.go`（例: `sequence/sequence_test.go`）にケースを追加し、状態遷移と表示メッセージの双方を検証します。ローカルでは `go test -cover ./...` を実行してカバレッジの変化を把握し、下がった場合は PR で理由と補填計画を明示してください。

## コミットとプルリクエストの指針
コミットメッセージは「window close on submit」「battle initializeを切り出してみる」のように短く命令形でまとめ、72 文字以内を目安にします。必要に応じて日本語で背景を補足してください。PR ではゲームプレイへの影響、実行したテストコマンド、UI 変更がある場合のスクリーンショットや GIF を記載し、関連 Issue や変更した YAML データを明確にリンクします。

## データとデバッグのヒント
`data/` の YAML は起動時に読み込まれるため、スキーマを変えるときは `invoke/export.go` のヘルパーで整合性を確認してください。重い検証を行う際は `debug/memory_leak_detector.go` を有効化して新しいウィジェットがリークを起こしていないかをチェックし、一時的なデバッグコードはビルドタグや明示的なコミットで管理します。
